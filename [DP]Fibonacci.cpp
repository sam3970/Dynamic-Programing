#include <iostream>
#include <vector>

using namespace std;

vector<int> vc;

int fibonacci(int num)
{
	if (num == 0)
	{
		return 0;
	}
	
	if (num == 1)
	{
		return 1;
	}
	
	/* Dynamic Programming apply part */
	if (vc[num] != -1)
	{
		ret
	}
}
